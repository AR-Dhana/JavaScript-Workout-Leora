// add two values passing the arguments //
function add(a, b) {
    return a + b;
}
console.log(add(4, 5));

// arrow function to add two values passing the arguments //

let newArrowFunction = (a, b) => { return a + b };
console.log(newArrowFunction(4, 6)); 